pipeline {
    agent any

    tools {
        maven 'Maven 3' // Replace 'Maven 3' with the actual name of your Maven tool in Jenkins
    }

    stages {
        stage('Checkout') {
            steps {
                script {
                    git branch: 'yosra', url: 'https://github.com/Si-aymen/Courzello.git'
                }
            }
        }

        stage('Build') {
            steps {
                dir('backendPidev') {
                    sh 'mvn clean install -Dmaven.test.skip=true'
                }
            }
        }

        stage('Test') {
            steps {
                dir('backendPidev') {
                    sh 'mvn te
